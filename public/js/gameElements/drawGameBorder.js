/**
 * Draws a grey border around the entire game canvas to denote the boundary of the game field.
 * @param {CanvasRenderingContext2D} ctx - The canvas rendering context.
 * @param {HTMLCanvasElement} gameCanvas - The canvas element where the game is rendered.
 */
export function drawGameBorder(ctx, gameCanvas) {
}